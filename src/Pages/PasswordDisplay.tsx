import React from 'react'
import {
    useColorMode,
} from "@chakra-ui/react";
export function PasswordDisplay() {
    const {colorMode} = useColorMode()
    return(
        <>
            Goofy ahh comp: PasswordDisplay
            
        </>
    )
}